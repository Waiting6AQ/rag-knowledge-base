# docker compose 配置模板：复制为 .env 并填入真实值（.env 不提交）
# 启动：docker compose up -d --build
DB_PASSWORD=
DASHSCOPE_API_KEY=
# LLM 模型名（可选，不填则用 docker-compose.yml 里的默认值）
# 换模型：改这行 → docker compose up -d（重建容器即可，无需重新 build 镜像）
LLM_MODEL_NAME=openai:qwen3.8-2.4t-a95b
